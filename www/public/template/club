<div id="club-{id}" class="media">
	<div class="media-left">
		<img class="media-object" data-src="..." alt="Generic placeholder image">
	</div>

	<div class="media-body">
		<h3 class="media-heading">{name}</h3>

		<p>{description}</p>

		<hr>

		<p><div class="form-group">
			<input id="club-checkbox-{city}-{id}" type="checkbox" autocomplete="off" />

			<div class="btn-group pull-right">
				<label for="club-checkbox-{city}-{id}" class="btn btn-default btn-xs">
					<span class="glyphicon glyphicon-ok"></span>
					<span></span>
				</label>

				<label for="club-checkbox-{city}-{id}" class="btn btn-default btn-xs active">
					Add to my TravelGuide
				</label>
			</div>
		</div></p>
	</div>
</div>
